# Auto-Explainer SaaS v0.0.1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Stand up an **internal alpha** of Auto-Explainer SaaS — text topic in → 30s mp4 out, single template, basic auth, **no billing**.

**Architecture:** Monolithic Next.js 14 (App Router) hosting both web + control API. A sibling Node.js worker process (`npm run worker`) consumes BullMQ jobs and runs the pipeline (LLM → HTML → record → encode). State lives in Postgres; intermediate artifacts on local FS (no MinIO in v0.0.1). Single template (30s beat5) hardcoded; multi-source input / multi-tenant deferred to v0.5.

**Tech Stack:** Next.js 14 + TypeScript, Drizzle ORM + Postgres 16, Redis 7 + BullMQ, Claude API (Sonnet 4.5), puppeteer-core + system Chrome, Playwright (probe + tests), ffmpeg-static, pino (logging), vitest (unit), Zod (env + IO validation).

**Acceptance (per spec §7.5):** Browser form submit "RAG 原理" → ≤ 15 min wall → downloadable `video.mp4` exists on disk + status `done` in DB.

---

## File Structure

```
D:\项目\系统\
├── app/                            # Next.js app router
│   ├── layout.tsx                  # Root layout
│   ├── page.tsx                    # Submit form (homepage)
│   ├── jobs/[id]/page.tsx          # Status + download page
│   └── api/
│       ├── jobs/route.ts           # POST /api/jobs (JobIntake)
│       └── jobs/[id]/route.ts      # GET /api/jobs/[id] (JobStatus)
│                                   # GET /api/jobs/[id]/download/[kind] (v0.0.1)
├── lib/
│   ├── env.ts                      # Zod-validated process env
│   ├── db.ts                       # Drizzle client
│   ├── schema.ts                   # Drizzle schema (jobs, job_events)
│   ├── queue.ts                    # BullMQ queue + helpers
│   ├── auth.ts                     # HTTP basic auth middleware
│   ├── llm.ts                      # Claude API client (with retry)
│   ├── templates/
│   │   └── beat5-30s.html          # 30s抖音科普模板（你数据库里的）
│   └── token-parser.ts             # Parse Assistant JSON to typed shape
├── worker/
│   ├── index.ts                    # Worker entry: `npm run worker`
│   ├── pipeline.ts                 # Phase runner (orchestrator)
│   └── phases/
│       ├── outline.ts              # OutlinePlanner + QG-plan stub
│       ├── script.ts               # ScriptWriter
│       ├── html.ts                 # TemplateSelector + HtmlRenderer
│       ├── probe.ts                # ProbeChecker
│       ├── record.ts               # Recorder (puppeteer-core + CDP)
│       ├── fps.ts                  # FpsCalculator (mtime 算法)
│       └── encode.ts               # Encoder (ffmpeg-static)
├── tests/
│   ├── helpers/
│   │   ├── db.ts                   # Test DB setup
│   │   └── queue.ts                # In-memory bullmq for tests
│   ├── unit/
│   │   ├── env.test.ts
│   │   ├── token-parser.test.ts
│   │   ├── fps.test.ts
│   │   └── encoder.test.ts
│   └── integration/
│       ├── api-jobs.test.ts
│       └── pipeline-smoke.test.ts
├── drizzle/                        # Migrations
│   └── 0000_init.sql               # Generated by drizzle-kit
├── storage/                        # v0.0.1 本地 fs 落地（v0.5 换 MinIO）
│   └── .gitkeep
├── docker-compose.yml              # Postgres + Redis for dev
├── drizzle.config.ts
├── next.config.mjs
├── package.json
├── tsconfig.json
├── vitest.config.ts
├── .env.example
├── .gitignore
└── docs/
    └── superpowers/
        ├── specs/2026-07-25-auto-explainer-saas-design.md
        └── plans/2026-07-25-auto-explainer-saas-v0_0_1.md   # ← 本文件
```

**为什么 monorepo 单仓**: v0.0.1 阶段不分 packages（你"先跑通再拆"的方法论 #6）。v0.5 起拆 `apps/web` 和 `apps/worker` 两包。

---

## Phase 1 — Bootstrap (Tasks 1-5)

### Task 1: 项目初始化 + 工具链

**Files:**
- Create: `package.json`, `tsconfig.json`, `next.config.mjs`, `.gitignore`, `.env.example`, `docker-compose.yml`

- [ ] **Step 1: git init**

```bash
cd "D:/项目/系统"
git init
git checkout -b feat/v0_0_1
```

- [ ] **Step 2: 创建 `package.json`**

```json
{
  "name": "auto-explainer-saas",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "next dev -p 3000",
    "build": "next build",
    "start": "next start -p 3000",
    "worker": "tsx watch worker/index.ts",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "next lint"
  },
  "dependencies": {
    "next": "^14.2.0",
    "react": "^18.3.0",
    "react-dom": "^18.3.0",
    "drizzle-orm": "^0.30.0",
    "pg": "^8.11.0",
    "bullmq": "^5.7.0",
    "ioredis": "^5.4.0",
    "zod": "^3.23.0",
    "bull-board": "^5.13.0",
    "pino": "^9.0.0",
    "pino-pretty": "^11.0.0",
    "@anthropic-ai/sdk": "^0.27.0",
    "puppeteer-core": "^22.0.0",
    "playwright": "^1.44.0",
    "ffmpeg-static": "^5.1.0",
    "@ffmpeg-installer/ffmpeg": "^1.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.12.0",
    "@types/react": "^18.3.0",
    "@types/pg": "^8.11.0",
    "typescript": "^5.4.0",
    "tsx": "^4.7.0",
    "drizzle-kit": "^0.20.0",
    "vitest": "^1.6.0",
    "@vitest/ui": "^1.6.0",
    "eslint": "^8.57.0",
    "eslint-config-next": "^14.2.0"
  }
}
```

- [ ] **Step 3: 创建 `tsconfig.json`**

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": false,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "preserve",
    "incremental": true,
    "plugins": [{ "name": "next" }],
    "baseUrl": ".",
    "paths": { "@/*": ["./*"] }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "exclude": ["node_modules"]
}
```

- [ ] **Step 4: 创建 `next.config.mjs`**

```js
/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: { serverActions: { bodySizeLimit: '50mb' } },
  // Worker process is sibling, not bundled with Next.js
  webpack: (config) => {
    config.externals = config.externals || [];
    return config;
  },
};
export default nextConfig;
```

- [ ] **Step 5: 创建 `.gitignore`**

```
node_modules/
.next/
out/
.env*
!.env.example
storage/jobs/*
!storage/jobs/.gitkeep
*.log
.DS_Store
coverage/
.tsbuildinfo
test-results/
playwright-report/
.idea/
.vscode/
```

- [ ] **Step 6: 创建 `.env.example`**

```env
# Postgres
DATABASE_URL=postgres://postgres:postgres@localhost:5432/aesaas

# Redis
REDIS_URL=redis://localhost:6379

# Claude API
ANTHROPIC_API_KEY=sk-ant-...

# Worker auth (HTTP basic, v0.0.1 only)
BASIC_AUTH_USER=admin
BASIC_AUTH_PASS=changeme

# Logging
LOG_LEVEL=info
NODE_ENV=development
```

- [ ] **Step 7: 创建 `docker-compose.yml`**

```yaml
version: '3.9'
services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: aesaas
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
volumes:
  pgdata:
```

- [ ] **Step 8: 安装依赖**

```bash
cd "D:/项目/系统"
npm install
```

Expected: 依赖装完，`node_modules/.bin/next` 存在。

- [ ] **Step 9: 把 `.env.example` 拷成 `.env.local` 并填值**

```bash
cp .env.example .env.local
# 编辑 .env.local，至少填 ANTHROPIC_API_KEY
```

- [ ] **Step 10: 启动 Postgres + Redis + 创建 gitkeep**

```bash
docker compose up -d
mkdir -p storage/jobs
touch storage/jobs/.gitkeep
```

Expected: `docker compose ps` 显示 postgres + redis 都 healthy。

- [ ] **Step 11: Commit**

```bash
git add -A
git commit -m "chore: bootstrap project skeleton (next + drizzle + bullmq + ts)"
```

---

### Task 2: 环境变量 Zod 校验 + 基础 logger

**Files:**
- Create: `lib/env.ts`, `lib/logger.ts`, `tests/unit/env.test.ts`

- [ ] **Step 1: 写失败的测试 `tests/unit/env.test.ts`**

```typescript
import { describe, it, expect } from 'vitest';
import { envSchema } from '@/lib/env';

describe('envSchema', () => {
  it('parses a complete valid env', () => {
    const result = envSchema.parse({
      DATABASE_URL: 'postgres://x',
      REDIS_URL: 'redis://x',
      ANTHROPIC_API_KEY: 'sk-ant-x',
      BASIC_AUTH_USER: 'admin',
      BASIC_AUTH_PASS: 'pw',
      NODE_ENV: 'development',
      LOG_LEVEL: 'info',
    });
    expect(result.DATABASE_URL).toBe('postgres://x');
    expect(result.NODE_ENV).toBe('development');
  });

  it('throws when required key is missing', () => {
    expect(() =>
      envSchema.parse({
        DATABASE_URL: 'postgres://x',
        // missing REDIS_URL, ANTHROPIC_API_KEY etc.
      }),
    ).toThrow();
  });
});
```

- [ ] **Step 2: 跑测试，确认它 fail**

```bash
npm run test -- tests/unit/env.test.ts
```

Expected: FAIL — "Cannot find module '@/lib/env'"。

- [ ] **Step 3: 创建 `lib/env.ts`**

```typescript
import { z } from 'zod';

export const envSchema = z.object({
  DATABASE_URL: z.string().url(),
  REDIS_URL: z.string().url(),
  ANTHROPIC_API_KEY: z.string().min(10),
  BASIC_AUTH_USER: z.string().min(1),
  BASIC_AUTH_PASS: z.string().min(1),
  NODE_ENV: z.enum(['development', 'production', 'test']).default('development'),
  LOG_LEVEL: z.enum(['fatal', 'error', 'warn', 'info', 'debug', 'trace']).default('info'),
});

// 用单例 + 缓存，避免每次 hot reload 重复 parse
let cached: z.infer<typeof envSchema> | null = null;

export function getEnv() {
  if (cached) return cached;
  const parsed = envSchema.safeParse(process.env);
  if (!parsed.success) {
    console.error('❌ Invalid env:', parsed.error.flatten().fieldErrors);
    throw new Error('Invalid environment');
  }
  cached = parsed.data;
  return cached;
}
```

- [ ] **Step 4: 创建 `lib/logger.ts`**

```typescript
import pino from 'pino';
import { getEnv } from './env';

const env = getEnv();

export const logger = pino({
  level: env.LOG_LEVEL,
  transport: env.NODE_ENV === 'development'
    ? { target: 'pino-pretty', options: { translateTime: 'SYS:HH:MM:ss' } }
    : undefined,
});
```

- [ ] **Step 5: 跑测试，确认它 pass**

```bash
npm run test -- tests/unit/env.test.ts
```

Expected: PASS — 2/2。

- [ ] **Step 6: Commit**

```bash
git add lib/env.ts lib/logger.ts tests/unit/env.test.ts
git commit -m "feat(lib): zod env schema + pino logger with singleton cache"
```

---

### Task 3: Postgres + Drizzle schema (jobs 表)

**Files:**
- Create: `lib/db.ts`, `lib/schema.ts`, `drizzle.config.ts`, `drizzle/0000_init.sql`, `tests/helpers/db.ts`, `tests/integration/api-jobs.test.ts` (stub)

- [ ] **Step 1: 创建 `drizzle.config.ts`**

```typescript
import type { Config } from 'drizzle-kit';

export default {
  schema: './lib/schema.ts',
  out: './drizzle',
  dialect: 'postgresql',
  dbCredentials: {
    url: process.env.DATABASE_URL ?? 'postgres://postgres:postgres@localhost:5432/aesaas',
  },
} satisfies Config;
```

- [ ] **Step 2: 创建 `lib/schema.ts`**

```typescript
import { pgTable, uuid, text, timestamp, integer, jsonb, varchar, pgEnum } from 'drizzle-orm/pg-core';

// 阶段枚举（spec §3.1）
export const phaseEnum = pgEnum('phase', [
  'pending', 'planning', 'planning_done', 'building', 'html_ready',
  'probing', 'recording', 'recording_done', 'encoding', 'tts_caption',
  'ppt_retro', 'finalize', 'done', 'failed', 'dead',
]);

export const inputTypeEnum = pgEnum('input_type', ['text', 'url', 'doc']);

// v0.0.1 只用 text，其他 enum 值预留

export const jobs = pgTable('jobs', {
  id: uuid('id').defaultRandom().primaryKey(),
  userId: varchar('user_id', { length: 64 }).notNull().default('admin'), // v0.0.1 hardcoded
  status: varchar('status', { length: 32 }).notNull().default('pending'),
  phase: phaseEnum('phase').notNull().default('pending'),
  attempts: integer('attempts').notNull().default(0),
  inputType: inputTypeEnum('input_type').notNull(),
  inputPayload: jsonb('input_payload').notNull(),  // { topic: string } for v0.0.1
  templateId: varchar('template_id', { length: 64 }),
  designTokens: jsonb('design_tokens'),
  totalCostCents: integer('total_cost_cents').notNull().default(0),
  startedAt: timestamp('started_at').withTimezone(),
  finishedAt: timestamp('finished_at').withTimezone(),
  lastError: jsonb('last_error'),
  createdAt: timestamp('created_at').withTimezone().notNull().defaultNow(),
  updatedAt: timestamp('updated_at').withTimezone().notNull().defaultNow(),
});

export const jobEvents = pgTable('job_events', {
  id: uuid('id').defaultRandom().primaryKey(),
  jobId: uuid('job_id').notNull().references(() => jobs.id, { onDelete: 'cascade' }),
  phase: varchar('phase', { length: 32 }).notNull(),
  event: varchar('event', { length: 64 }).notNull(),
  payload: jsonb('payload'),
  at: timestamp('at').withTimezone().notNull().defaultNow(),
});

export const jobArtifacts = pgTable('job_artifacts', {
  id: uuid('id').defaultRandom().primaryKey(),
  jobId: uuid('job_id').notNull().references(() => jobs.id, { onDelete: 'cascade' }),
  kind: varchar('kind', { length: 32 }).notNull(),  // html / mp4 / mp3 / srt / pptx / retrospective
  storagePath: text('storage_path').notNull(),
  sizeBytes: integer('size_bytes'),
  sha256: varchar('sha256', { length: 64 }),
  createdAt: timestamp('created_at').withTimezone().notNull().defaultNow(),
});
```

- [ ] **Step 3: 生成 + 执行迁移**

```bash
npx drizzle-kit generate
npx drizzle-kit migrate
```

Expected: `drizzle/0000_init.sql` 生成，Postgres 出现 `jobs` / `job_events` / `job_artifacts` 三表 + 2 个 enum。

- [ ] **Step 4: 创建 `lib/db.ts`**

```typescript
import { drizzle } from 'drizzle-orm/node-postgres';
import { Pool } from 'pg';
import { getEnv } from './env';
import * as schema from './schema';
import { logger } from './logger';

let pool: Pool | null = null;

export function getPool() {
  if (pool) return pool;
  const env = getEnv();
  pool = new Pool({
    connectionString: env.DATABASE_URL,
    max: 10,
  });
  pool.on('error', (err) => logger.error({ err }, 'pg pool error'));
  return pool;
}

export function getDb() {
  return drizzle(getPool(), { schema });
}

export type Db = ReturnType<typeof getDb>;
```

- [ ] **Step 5: 写测试 `tests/helpers/db.ts`**（测试用同一库，独立 schema）

```typescript
import { drizzle } from 'drizzle-orm/node-postgres';
import { Pool } from 'pg';

export function makeTestDb(url: string) {
  const pool = new Pool({ connectionString: url, max: 2 });
  return { pool, db: drizzle(pool) };
}
```

- [ ] **Step 6: 写 integration 测试 `tests/integration/api-jobs.test.ts`**（先空，仅占位）

```typescript
import { describe, it, expect } from 'vitest';

describe('placeholder', () => {
  it('truthy', () => {
    expect(1).toBe(1);
  });
});
```

- [ ] **Step 7: 跑测试，确认 pass**

```bash
npm run test
```

Expected: 3+1 测试全 PASS（env.test.ts + placeholder）。

- [ ] **Step 8: Commit**

```bash
git add drizzle/ drizzle.config.ts lib/db.ts lib/schema.ts tests/
git commit -m "feat(db): drizzle schema (jobs/job_events/job_artifacts) + initial migration"
```

---

### Task 4: Redis + BullMQ 队列

**Files:**
- Create: `lib/queue.ts`, `tests/helpers/queue.ts`

- [ ] **Step 1: 创建 `lib/queue.ts`**

```typescript
import { Queue, Worker, type Processor } from 'bullmq';
import IORedis from 'ioredis';
import { getEnv } from './env';
import { logger } from './logger';

let connection: IORedis | null = null;

export function getRedis() {
  if (connection) return connection;
  const env = getEnv();
  connection = new IORedis(env.REDIS_URL, { maxRetriesPerRequest: null });
  connection.on('error', (err) => logger.error({ err }, 'redis error'));
  return connection;
}

export const JOB_QUEUE_NAME = 'video-jobs';

let queueInstance: Queue | null = null;

export function getJobQueue(): Queue {
  if (queueInstance) return queueInstance;
  queueInstance = new Queue(JOB_QUEUE_NAME, { connection: getRedis() });
  return queueInstance;
}

export interface JobData {
  jobId: string;
  phase: string;
}

export function createWorker(processor: Processor<JobData>) {
  return new Worker<JobData>(JOB_QUEUE_NAME, processor, {
    connection: getRedis(),
    concurrency: 1,  // v0.0.1 单 worker 进程
  });
}
```

- [ ] **Step 2: 创建 `tests/helpers/queue.ts`**（独立测试用 queue name）

```typescript
import { Queue, Worker } from 'bullmq';
import IORedis from 'ioredis';
import { randomUUID } from 'crypto';

export function makeTestQueue() {
  const name = `test-${randomUUID()}`;
  const conn = new IORedis(process.env.REDIS_URL ?? 'redis://localhost:6379', {
    maxRetriesPerRequest: null,
  });
  const queue = new Queue(name, { connection: conn });
  return {
    queue,
    async cleanup() {
      await queue.obliterate({ force: true });
      await conn.quit();
    },
  };
}
```

- [ ] **Step 3: Commit**

```bash
git add lib/queue.ts tests/helpers/queue.ts
git commit -m "feat(queue): bullmq wrapper + redis singleton + worker factory"
```

---

### Task 5: HTTP Basic Auth 中间件 + JobEvent helper

**Files:**
- Create: `lib/auth.ts`, `lib/job-events.ts`, `tests/unit/auth.test.ts`

- [ ] **Step 1: 写失败测试 `tests/unit/auth.test.ts`**

```typescript
import { describe, it, expect } from 'vitest';
import { createBasicAuthMiddleware } from '@/lib/auth';

describe('basicAuth', () => {
  const mw = createBasicAuthMiddleware({ user: 'admin', pass: 'pw' });

  it('rejects when auth header missing', () => {
    const req = new Request('http://x/api');
    expect(mw(req)).toBeNull();
  });

  it('accepts when Basic header matches', () => {
    const token = Buffer.from('admin:pw').toString('base64');
    const req = new Request('http://x/api', { headers: { authorization: `Basic ${token}` } });
    expect(mw(req)).toBe('admin');
  });

  it('rejects wrong password', () => {
    const token = Buffer.from('admin:wrong').toString('base64');
    const req = new Request('http://x/api', { headers: { authorization: `Basic ${token}` } });
    expect(mw(req)).toBeNull();
  });
});
```

- [ ] **Step 2: 跑测试，期望 fail**

```bash
npm run test -- tests/unit/auth.test.ts
```

Expected: FAIL — module 未找到。

- [ ] **Step 3: 实现 `lib/auth.ts`**

```typescript
import { timingSafeEqual } from 'crypto';

export interface BasicAuthConfig {
  user: string;
  pass: string;
}

export function createBasicAuthMiddleware(cfg: BasicAuthConfig) {
  return (req: Request): string | null => {
    const header = req.headers.get('authorization');
    if (!header?.startsWith('Basic ')) return null;
    const decoded = Buffer.from(header.slice(6), 'base64').toString('utf8');
    const [user, pass] = decoded.split(':');
    if (!user || !pass) return null;
    const userOk = timingSafeEqual(Buffer.from(user), Buffer.from(cfg.user));
    const passOk = timingSafeEqual(Buffer.from(pass), Buffer.from(cfg.pass));
    return userOk && passOk ? user : null;
  };
}

export function unauthorizedResponse(): Response {
  return new Response('Unauthorized', {
    status: 401,
    headers: { 'WWW-Authenticate': 'Basic realm="auto-explainer"' },
  });
}
```

- [ ] **Step 4: 创建 `lib/job-events.ts`**

```typescript
import { getDb } from './db';
import { jobEvents } from './schema';

export async function recordEvent(
  jobId: string,
  phase: string,
  event: string,
  payload?: unknown,
) {
  const db = getDb();
  await db.insert(jobEvents).values({
    jobId,
    phase,
    event,
    payload: payload as any,
  });
}
```

- [ ] **Step 5: 跑测试，期望 pass**

```bash
npm run test
```

Expected: 4+1 测试 PASS。

- [ ] **Step 6: Commit**

```bash
git add lib/auth.ts lib/job-events.ts tests/unit/auth.test.ts
git commit -m "feat(auth): http basic auth middleware + job event helper"
```

---

## Phase 2 — Web Layer (Tasks 6-9)

### Task 6: POST `/api/jobs` (JobIntake)

**Files:**
- Create: `app/api/jobs/route.ts`, `tests/integration/api-jobs.test.ts`

- [ ] **Step 1: 写失败测试 `tests/integration/api-jobs.test.ts`**

```typescript
import { describe, it, expect, beforeAll, afterAll } from 'vitest';
import { POST as createJob } from '@/app/api/jobs/route';
import { getDb } from '@/lib/db';
import { jobs } from '@/lib/schema';

const token = Buffer.from('admin:changeme').toString('base64');
const authHeader = { authorization: `Basic ${token}` };

describe('POST /api/jobs', () => {
  it('rejects without auth', async () => {
    const req = new Request('http://x/api/jobs', {
      method: 'POST',
      headers: { 'content-type': 'application/json' },
      body: JSON.stringify({ inputType: 'text', topic: 'RAG 原理' }),
    });
    const res = await createJob(req);
    expect(res.status).toBe(401);
  });

  it('creates a job with valid auth + text input', async () => {
    const req = new Request('http://x/api/jobs', {
      method: 'POST',
      headers: { ...authHeader, 'content-type': 'application/json' },
      body: JSON.stringify({ inputType: 'text', topic: 'RAG 原理' }),
    });
    const res = await createJob(req);
    expect(res.status).toBe(201);
    const { jobId } = await res.json();

    // sanity check DB
    const db = getDb();
    const rows = await db.select().from(jobs).where((j: any) => j.id as any);
    expect(rows.length).toBeGreaterThanOrEqual(0);  // 至少没崩
    expect(jobId).toMatch(/^[0-9a-f-]{36}$/);
  });

  it('returns 400 for missing topic', async () => {
    const req = new Request('http://x/api/jobs', {
      method: 'POST',
      headers: { ...authHeader, 'content-type': 'application/json' },
      body: JSON.stringify({ inputType: 'text' }),
    });
    const res = await createJob(req);
    expect(res.status).toBe(400);
  });

  it('returns 400 for empty topic', async () => {
    const req = new Request('http://x/api/jobs', {
      method: 'POST',
      headers: { ...authHeader, 'content-type': 'application/json' },
      body: JSON.stringify({ inputType: 'text', topic: '' }),
    });
    const res = await createJob(req);
    expect(res.status).toBe(400);
  });

  it('returns 400 for topic > 500 chars', async () => {
    const req = new Request('http://x/api/jobs', {
      method: 'POST',
      headers: { ...authHeader, 'content-type': 'application/json' },
      body: JSON.stringify({ inputType: 'text', topic: 'a'.repeat(501) }),
    });
    const res = await createJob(req);
    expect(res.status).toBe(400);
  });
});
```

- [ ] **Step 2: 跑测试，期望 fail**

```bash
npm run test -- tests/integration/api-jobs.test.ts
```

Expected: FAIL — module 未找到。

- [ ] **Step 3: 实现 `app/api/jobs/route.ts`**

```typescript
import { NextResponse } from 'next/server';
import { z } from 'zod';
import { getDb } from '@/lib/db';
import { jobs } from '@/lib/schema';
import { createBasicAuthMiddleware, unauthorizedResponse } from '@/lib/auth';
import { getJobQueue } from '@/lib/queue';
import { recordEvent } from '@/lib/job-events';
import { getEnv } from '@/lib/env';

const InputSchema = z.object({
  inputType: z.enum(['text']),  // v0.0.1 only text
  topic: z.string().min(1).max(500),
});

const authMw = createBasicAuthMiddleware({
  user: getEnv().BASIC_AUTH_USER,
  pass: getEnv().BASIC_AUTH_PASS,
});

export async function POST(req: Request) {
  const user = authMw(req);
  if (!user) return unauthorizedResponse();

  let json: unknown;
  try {
    json = await req.json();
  } catch {
    return NextResponse.json({ error: 'invalid_json' }, { status: 400 });
  }

  const parsed = InputSchema.safeParse(json);
  if (!parsed.success) {
    return NextResponse.json(
      { error: 'invalid_input', details: parsed.error.flatten() },
      { status: 400 },
    );
  }

  const db = getDb();
  const [job] = await db.insert(jobs).values({
    userId: user,
    status: 'pending',
    phase: 'pending',
    inputType: parsed.data.inputType,
    inputPayload: { topic: parsed.data.topic },
  }).returning();

  // 入队
  await getJobQueue().add('pipeline', { jobId: job.id, phase: 'pending' });
  await recordEvent(job.id, 'pending', 'created', parsed.data);

  return NextResponse.json({ jobId: job.id }, { status: 201 });
}
```

- [ ] **Step 4: 跑测试，期望 pass**

```bash
npm run test -- tests/integration/api-jobs.test.ts
```

Expected: 5/5 PASS。

> 注：vitest 默认不会起 Next dev server。这测试是直接调 route handler function。

- [ ] **Step 5: Commit**

```bash
git add app/api/jobs/route.ts tests/integration/api-jobs.test.ts
git commit -m "feat(api): POST /api/jobs with basic auth + zod input + queue enqueue"
```

---

### Task 7: GET `/api/jobs/[id]` (JobStatus)

**Files:**
- Create: `app/api/jobs/[id]/route.ts`

- [ ] **Step 1: 实现 route handler**

```typescript
import { NextResponse } from 'next/server';
import { eq } from 'drizzle-orm';
import { getDb } from '@/lib/db';
import { jobs } from '@/lib/schema';
import { createBasicAuthMiddleware, unauthorizedResponse } from '@/lib/auth';
import { getEnv } from '@/lib/env';

const authMw = createBasicAuthMiddleware({
  user: getEnv().BASIC_AUTH_USER,
  pass: getEnv().BASIC_AUTH_PASS,
});

export async function GET(
  req: Request,
  { params }: { params: { id: string } },
) {
  if (!authMw(req)) return unauthorizedResponse();

  const db = getDb();
  const [job] = await db.select().from(jobs).where(eq(jobs.id, params.id)).limit(1);
  if (!job) return NextResponse.json({ error: 'not_found' }, { status: 404 });

  return NextResponse.json({
    id: job.id,
    status: job.status,
    phase: job.phase,
    attempts: job.attempts,
    inputType: job.inputType,
    startedAt: job.startedAt,
    finishedAt: job.finishedAt,
    lastError: job.lastError,
  });
}
```

- [ ] **Step 2: Commit**

```bash
git add app/api/jobs/[id]/route.ts
git commit -m "feat(api): GET /api/jobs/[id] for status polling"
```

---

### Task 8: Submit UI (homepage)

**Files:**
- Create: `app/layout.tsx`, `app/page.tsx`, `app/globals.css`

- [ ] **Step 1: 创建 `app/globals.css`**

```css
:root {
  --bg: #0d1117;
  --fg: #c9d1d9;
  --accent: #58a6ff;
  --muted: #8b949e;
}
* { box-sizing: border-box; margin: 0; padding: 0; }
body {
  font-family: ui-sans-serif, system-ui, "Microsoft YaHei";
  background: var(--bg);
  color: var(--fg);
  min-height: 100vh;
  padding: 40px 20px;
}
main { max-width: 640px; margin: 0 auto; }
h1 { font-size: 32px; margin-bottom: 24px; color: var(--accent); }
form { display: flex; flex-direction: column; gap: 12px; }
label { font-size: 14px; color: var(--muted); }
input, textarea {
  background: #161b22; border: 1px solid #30363d; border-radius: 6px;
  padding: 10px 12px; color: var(--fg); font-size: 16px;
}
button {
  background: var(--accent); color: #0d1117; font-weight: 600;
  border: none; border-radius: 6px; padding: 10px 20px; cursor: pointer;
}
button:disabled { opacity: 0.5; cursor: not-allowed; }
.result { margin-top: 20px; padding: 12px; background: #161b22; border-radius: 6px; }
```

- [ ] **Step 2: 创建 `app/layout.tsx`**

```tsx
import './globals.css';
import type { ReactNode } from 'react';

export const metadata = {
  title: 'Auto-Explainer · v0.0.1 alpha',
};

export default function RootLayout({ children }: { children: ReactNode }) {
  return (
    <html lang="zh">
      <body>{children}</body>
    </html>
  );
}
```

- [ ] **Step 3: 创建 `app/page.tsx`**

```tsx
'use client';

import { useState } from 'react';

export default function Home() {
  const [topic, setTopic] = useState('');
  const [submitting, setSubmitting] = useState(false);
  const [result, setResult] = useState<{ jobId: string } | { error: string } | null>(null);

  async function onSubmit(e: React.FormEvent) {
    e.preventDefault();
    setSubmitting(true);
    setResult(null);
    try {
      const res = await fetch('/api/jobs', {
        method: 'POST',
        headers: {
          'content-type': 'application/json',
          authorization: `Basic ${Buffer.from(`${process.env.NEXT_PUBLIC_BASIC_AUTH_USER ?? 'admin'}:${process.env.NEXT_PUBLIC_BASIC_AUTH_PASS ?? 'changeme'}`).toString('base64')}`,
        },
        body: JSON.stringify({ inputType: 'text', topic }),
      });
      const data = await res.json();
      if (!res.ok) {
        setResult({ error: data.error ?? 'unknown' });
      } else {
        setResult({ jobId: data.jobId });
      }
    } catch (err: any) {
      setResult({ error: err.message });
    } finally {
      setSubmitting(false);
    }
  }

  return (
    <main>
      <h1>Auto-Explainer · v0.0.1 alpha</h1>
      <p style={{ color: 'var(--muted)', marginBottom: 24 }}>
        内部 alpha。提交一个 topic，等 ~10 分钟拿到 mp4。
      </p>
      <form onSubmit={onSubmit}>
        <label htmlFor="topic">Topic 主题（中英都行，≤500 字）</label>
        <textarea
          id="topic"
          rows={3}
          value={topic}
          onChange={(e) => setTopic(e.target.value)}
          placeholder="例如：RAG 的工作原理"
          maxLength={500}
          required
        />
        <button type="submit" disabled={submitting || topic.length === 0}>
          {submitting ? '提交中…' : '开始生成'}
        </button>
      </form>
      {result && 'jobId' in result && (
        <div className="result">
          ✅ 已提交。jobId = <code>{result.jobId}</code>
          <br />
          <a href={`/jobs/${result.jobId}`}>查看进度 →</a>
        </div>
      )}
      {result && 'error' in result && (
        <div className="result" style={{ color: '#f85149' }}>❌ {result.error}</div>
      )}
    </main>
  );
}
```

- [ ] **Step 4: 手动 smoke: `npm run dev`，浏览器访问 `http://localhost:3000`**

```bash
npm run dev
```

Expected: 表单可见。POST 后看到 jobId 出现。**注意**：client side 用 buffer 编码 basic auth 在浏览器可见 —— v0.0.1 够用。

- [ ] **Step 5: Commit**

```bash
git add app/layout.tsx app/page.tsx app/globals.css
git commit -m "feat(ui): submit homepage with basic auth via client header"
```

---

### Task 9: Status page

**Files:**
- Create: `app/jobs/[id]/page.tsx`

- [ ] **Step 1: 创建页面**

```tsx
'use client';

import { useEffect, useState } from 'react';
import { useParams } from 'next/navigation';

interface Job {
  id: string;
  status: string;
  phase: string;
  attempts: number;
  startedAt: string | null;
  finishedAt: string | null;
  lastError: any;
}

export default function JobStatusPage() {
  const params = useParams<{ id: string }>();
  const [job, setJob] = useState<Job | null>(null);
  const [error, setError] = useState<string | null>(null);

  useEffect(() => {
    let cancelled = false;
    let timer: ReturnType<typeof setTimeout>;

    async function poll() {
      try {
        const res = await fetch(`/api/jobs/${params.id}`, {
          headers: {
            authorization: `Basic ${Buffer.from(`${process.env.NEXT_PUBLIC_BASIC_AUTH_USER ?? 'admin'}:${process.env.NEXT_PUBLIC_BASIC_AUTH_PASS ?? 'changeme'}`).toString('base64')}`,
          },
        });
        if (cancelled) return;
        if (res.status === 404) { setError('Job not found'); return; }
        const data = await res.json();
        setJob(data);
        if (data.status !== 'done' && data.status !== 'failed' && data.status !== 'dead') {
          timer = setTimeout(poll, 5000);
        }
      } catch (e: any) {
        setError(e.message);
      }
    }

    poll();
    return () => { cancelled = true; clearTimeout(timer); };
  }, [params.id]);

  if (error) return <main><h1>❌ {error}</h1></main>;
  if (!job) return <main><h1>loading…</h1></main>;

  return (
    <main>
      <h1>Job {job.id.slice(0, 8)}…</h1>
      <p><b>Status:</b> {job.status}</p>
      <p><b>Phase:</b> {job.phase}</p>
      <p><b>Attempts:</b> {job.attempts}</p>
      {job.lastError && (
        <div className="result" style={{ color: '#f85149' }}>
          <b>Last error:</b> <pre>{JSON.stringify(job.lastError, null, 2)}</pre>
        </div>
      )}
      {job.status === 'done' && (
        <div className="result">
          ✅ 完成。<a href={`/api/jobs/${job.id}/download/mp4`}>下载 mp4</a>
        </div>
      )}
    </main>
  );
}
```

- [ ] **Step 2: Commit**

```bash
git add app/jobs/[id]/page.tsx
git commit -m "feat(ui): job status page with 5s polling"
```

---

## Phase 3 — Pipeline Skeleton (Tasks 10-12)

### Task 10: Worker 入口

**Files:**
- Create: `worker/index.ts`

- [ ] **Step 1: 创建 worker entry**

```typescript
import { createWorker, type JobData } from '@/lib/queue';
import { runPipeline } from './pipeline';
import { logger } from '@/lib/logger';

const worker = createWorker(async (job) => {
  const { jobId, phase } = job.data;
  logger.info({ jobId, phase }, 'worker picked job');
  await runPipeline(jobId);
  return { ok: true };
});

worker.on('completed', (job) => {
  logger.info({ jobId: job.data.jobId, phase: job.data.phase }, 'job completed');
});

worker.on('failed', (job, err) => {
  logger.error({ jobId: job?.data.jobId, err: err.message }, 'job failed');
});

logger.info('worker started, waiting for jobs…');

// 优雅退出
process.on('SIGINT', async () => {
  logger.info('SIGINT received, closing worker…');
  await worker.close();
  process.exit(0);
});
```

- [ ] **Step 2: Commit (pipeline stub 还没实现，先空 import)**

```bash
git add worker/index.ts
git commit -m "feat(worker): worker entry point with bullmq consumer"
```

---

### Task 11: Pipeline runner + phase 状态机骨架

**Files:**
- Create: `worker/pipeline.ts`

- [ ] **Step 1: 实现**

```typescript
import { eq } from 'drizzle-orm';
import { getDb } from '@/lib/db';
import { jobs } from '@/lib/schema';
import { recordEvent } from '@/lib/job-events';
import { logger } from '@/lib/logger';
import { phaseOutline } from './phases/outline';
import { phaseScript } from './phases/script';
import { phaseHtml } from './phases/html';
import { phaseProbe } from './phases/probe';
import { phaseRecord } from './phases/record';
import { phaseEncode } from './phases/encode';

const PHASE_ORDER = [
  { name: 'planning_done', run: phaseOutline },     // 完成 planning，等价于 planning_done
  { name: 'html_ready',    run: phaseHtml },       // render + selector
  { name: 'probing',       run: phaseProbe },
  { name: 'recording_done',run: phaseRecord },
  { name: 'done',          run: phaseEncode },
];

export async function runPipeline(jobId: string) {
  const db = getDb();

  // 标记 startedAt
  await db.update(jobs).set({ startedAt: new Date(), status: 'running' })
    .where(eq(jobs.id, jobId));

  try {
    for (const step of PHASE_ORDER) {
      logger.info({ jobId, phase: step.name }, 'phase starting');
      await db.update(jobs).set({ phase: step.name as any, status: 'running', attempts: 1 })
        .where(eq(jobs.id, jobId));

      await step.run(jobId);
      await recordEvent(jobId, step.name, 'phase_completed');
    }

    await db.update(jobs).set({ status: 'done', finishedAt: new Date() })
      .where(eq(jobs.id, jobId));
  } catch (err: any) {
    logger.error({ jobId, err: err.message, stack: err.stack }, 'pipeline failed');
    await db.update(jobs).set({
      status: 'failed',
      finishedAt: new Date(),
      lastError: { message: err.message, stack: err.stack },
    }).where(eq(jobs.id, jobId));
    throw err;
  }
}
```

> v0.0.1 简化：所有阶段顺序跑，单 attempt，失败直接 `failed`。v0.5 起加 retry + 撞墙拐点（spec §4）。

- [ ] **Step 2: Commit（phases 模块还没建，先 commit runner）**

```bash
git add worker/pipeline.ts
git commit -m "feat(worker): pipeline runner with phase state machine stub"
```

---

### Task 12: 阶段接口 stub（6 个 phases 各返回 no-op）

**Files:**
- Create: `worker/phases/outline.ts`, `worker/phases/script.ts`, `worker/phases/html.ts`, `worker/phases/probe.ts`, `worker/phases/record.ts`, `worker/phases/encode.ts`

- [ ] **Step 1: 创建 6 个 phase stub**

每个文件相同模式：

```typescript
// worker/phases/outline.ts
import { logger } from '@/lib/logger';
export async function phaseOutline(jobId: string) {
  logger.info({ jobId }, '[stub] outline phase');
}
```

```typescript
// worker/phases/script.ts
import { logger } from '@/lib/logger';
export async function phaseScript(jobId: string) {
  logger.info({ jobId }, '[stub] script phase');
}
```

```typescript
// worker/phases/html.ts
import { logger } from '@/lib/logger';
export async function phaseHtml(jobId: string) {
  logger.info({ jobId }, '[stub] html phase');
}
```

```typescript
// worker/phases/probe.ts
import { logger } from '@/lib/logger';
export async function phaseProbe(jobId: string) {
  logger.info({ jobId }, '[stub] probe phase');
}
```

```typescript
// worker/phases/record.ts
import { logger } from '@/lib/logger';
export async function phaseRecord(jobId: string) {
  logger.info({ jobId }, '[stub] record phase');
}
```

```typescript
// worker/phases/encode.ts
import { logger } from '@/lib/logger';
export async function phaseEncode(jobId: string) {
  logger.info({ jobId }, '[stub] encode phase');
}
```

> ⚠️ 注意：phaseScript 和 phaseEncode 暂时没被 pipeline.ts 引用（pipeline 把 outline/script 合并成单 step 里 inline 调）。先建 stub 等 Phase 4 完善时再 inline 展开。

- [ ] **Step 2: Smoke 测试 — 起 worker 进程 + 提一个 job 看 stub 全跑通**

```bash
# terminal 1
npm run worker

# terminal 2: 通过 API 提交
curl -u admin:changeme -X POST http://localhost:3000/api/jobs \
  -H "content-type: application/json" \
  -d '{"inputType":"text","topic":"RAG 原理"}'
# → {"jobId":"..."}
```

Expected: worker 日志看到 6 个 stub phase 输出，最终 `status: done`。

- [ ] **Step 3: Commit**

```bash
git add worker/phases/
git commit -m "feat(worker): 6 phase stubs to verify pipeline wiring"
```

---

## Phase 4 — LLM Phases (Tasks 13-15)

### Task 13: Claude API 客户端

**Files:**
- Create: `lib/llm.ts`, `tests/unit/llm-error.test.ts`

- [ ] **Step 1: 写失败测试 `tests/unit/llm-error.test.ts`**

```typescript
import { describe, it, expect } from 'vitest';
import { parseAssistantJson } from '@/lib/llm';

describe('parseAssistantJson', () => {
  it('extracts JSON from ```json fences', () => {
    const raw = '```json\n{"a":1,"b":"x"}\n```';
    expect(parseAssistantJson(raw)).toEqual({ a: 1, b: 'x' });
  });

  it('parses raw JSON without fences', () => {
    expect(parseAssistantJson('{"k":"v"}')).toEqual({ k: 'v' });
  });

  it('throws on truly non-JSON input', () => {
    expect(() => parseAssistantJson('hello world')).toThrow();
  });

  it('strips leading prose before JSON block', () => {
    expect(parseAssistantJson('Here is the output:\n```json\n[1,2]\n```')).toEqual([1, 2]);
  });
});
```

- [ ] **Step 2: 跑测试 fail**

```bash
npm run test -- tests/unit/llm-error.test.ts
```

Expected: FAIL — 模块未找到。

- [ ] **Step 3: 实现 `lib/llm.ts`**

```typescript
import Anthropic from '@anthropic-ai/sdk';
import { getEnv } from './env';
import { logger } from './logger';

let client: Anthropic | null = null;

export function getAnthropic() {
  if (client) return client;
  const env = getEnv();
  client = new Anthropic({ apiKey: env.ANTHROPIC_API_KEY });
  return client;
}

export interface LlmMessage {
  role: 'user' | 'assistant' | 'system';
  content: string;
}

export async function callClaude(opts: {
  system?: string;
  messages: LlmMessage[];
  maxTokens?: number;
  model?: string;
}): Promise<string> {
  const env = getEnv();
  const client = getAnthropic();
  const params: any = {
    model: opts.model ?? 'claude-sonnet-4-5',
    max_tokens: opts.maxTokens ?? 4096,
    messages: opts.messages,
  };
  if (opts.system) params.system = opts.system;

  let lastErr: unknown;
  for (let attempt = 0; attempt < 3; attempt++) {
    try {
      const res = await client.messages.create(params);
      const block = res.content[0];
      if (block.type !== 'text') throw new Error('non-text response');
      return block.text;
    } catch (err: any) {
      lastErr = err;
      const status = err?.status;
      if (status && status >= 400 && status < 500 && status !== 429) break;  // 4xx 不重试
      const backoff = Math.min(2 ** attempt * 1000, 16000);
      logger.warn({ attempt, backoff, err: err.message }, 'claude retry');
      await new Promise((r) => setTimeout(r, backoff));
    }
  }
  throw lastErr;
}

// 从 Claude 文本里抠 JSON（容忍 ```json fence / 前置废话）
export function parseAssistantJson(raw: string): unknown {
  // 尝试 fence
  const fenceMatch = raw.match(/```(?:json)?\s*([\s\S]*?)```/);
  if (fenceMatch) return JSON.parse(fenceMatch[1].trim());
  // 尝试 raw
  const trimmed = raw.trim();
  const firstBrace = trimmed.indexOf('{');
  const firstBracket = trimmed.indexOf('[');
  const starts = [firstBrace, firstBracket].filter((i) => i >= 0);
  if (starts.length > 0) {
    const start = Math.min(...starts);
    return JSON.parse(trimmed.slice(start));
  }
  throw new Error('No JSON found');
}
```

- [ ] **Step 4: 跑测试 pass**

```bash
npm run test
```

Expected: 4+ 测试全 PASS。

- [ ] **Step 5: Commit**

```bash
git add lib/llm.ts tests/unit/llm-error.test.ts
git commit -m "feat(llm): anthropic client wrapper + JSON fence parser + retry"
```

---

### Task 14: OutlinePlanner + QG-plan 基础版

**Files:**
- Create: `worker/phases/outline.ts`（覆盖 stub）, `lib/prompts.ts`

- [ ] **Step 1: 创建 `lib/prompts.ts`**

```typescript
// 系统级头三条（spec §4.6）
export const SYSTEM_PROMPT_HEADER = `
你正在为 30 秒科普视频生成内容大纲。规则：
1. 撞墙拐点：同一方向最多 3 版；v3 不行回退 + 选择性吸收，不推倒重来。
2. 第 2 次输出不合规必向系统申请询问，不准直接装 OK 提交。
3. 任何改 html 的 JS 后必跑 probe-console，没 [无 ERR] 不准进入下一阶段。

输出必须是合法 JSON。
`.trim();

export const PLAN_PROMPT = (topic: string) => `
为这个 topic 做一个 30 秒抖音科普视频的大纲。

topic：${topic}

输出 JSON 格式（必须严格遵守）：
{
  "title": "<视频标题 12 字以内>",
  "audience": "<受众一句话，如 '零基础但被这个词刷屏的人'>",
  "target_seconds": 30,
  "beats": [
    { "id": "b1", "name": "钩子",       "duration_ms": 6000,  "purpose": "..." },
    { "id": "b2", "name": "定义",       "duration_ms": 6000,  "purpose": "..." },
    { "id": "b3", "name": "数字对比",   "duration_ms": 6000,  "purpose": "..." },
    { "id": "b4", "name": "真实例子",   "duration_ms": 6000,  "purpose": "..." },
    { "id": "b5", "name": "收尾关键词", "duration_ms": 6000,  "purpose": "..." }
  ],
  "visual_plan": [
    { "beat_id": "b1", "visual": "<钩子段的视觉描述，包含字号/字号对比/动效>" },
    { "beat_id": "b2", "visual": "..." },
    ...
  ]
}

约束：
- 5 个 beats 总和 = 30000ms（30s）
- 每个 beat 的 duration_ms 必须是 6000
- title 必须让人想点开（数字 + 反常识优先）
- visual 描述要具体到「80px 黑底白字、配色 #58a6ff、staggered 入场」这种 level
`.trim();
```

- [ ] **Step 2: 实现 `worker/phases/outline.ts`**

```typescript
import { eq } from 'drizzle-orm';
import { getDb } from '@/lib/db';
import { jobs } from '@/lib/schema';
import { callClaude, parseAssistantJson } from '@/lib/llm';
import { SYSTEM_PROMPT_HEADER, PLAN_PROMPT } from '@/lib/prompts';
import { logger } from '@/lib/logger';
import { z } from 'zod';
import fs from 'fs/promises';
import path from 'path';

const BeatSchema = z.object({
  id: z.string(),
  name: z.string(),
  duration_ms: z.number(),
  purpose: z.string(),
});

const VisualSchema = z.object({
  beat_id: z.string(),
  visual: z.string(),
});

const PlanSchema = z.object({
  title: z.string().min(2).max(30),
  audience: z.string(),
  target_seconds: z.literal(30),
  beats: z.array(BeatSchema).length(5),
  visual_plan: z.array(VisualSchema).length(5),
});

export type Plan = z.infer<typeof PlanSchema>;

export async function phaseOutline(jobId: string) {
  const db = getDb();
  const [job] = await db.select().from(jobs).where(eq(jobs.id, jobId)).limit(1);
  if (!job) throw new Error('job not found');
  const topic = (job.inputPayload as any).topic;

  logger.info({ jobId, topic }, 'outline phase starting');

  const raw = await callClaude({
    system: SYSTEM_PROMPT_HEADER,
    messages: [{ role: 'user', content: PLAN_PROMPT(topic) }],
    maxTokens: 2048,
  });

  const parsed = PlanSchema.safeParse(parseAssistantJson(raw));
  if (!parsed.success) {
    logger.warn({ jobId, issue: parsed.error.flatten() }, 'plan failed QG-plan v1 (structural)');
    throw new Error('plan failed quality gate');
  }
  // 简单长度检查（撞墙拐点法的早期版）
  if (parsed.data.title.length < 4) {
    throw new Error('plan title too short');
  }

  // 落盘 + 存 DB
  const planPath = path.join(process.cwd(), 'storage', 'jobs', jobId, 'plan.json');
  await fs.mkdir(path.dirname(planPath), { recursive: true });
  await fs.writeFile(planPath, JSON.stringify(parsed.data, null, 2), 'utf8');
  await db.update(jobs).set({ templateId: 'beat5-30s' }).where(eq(jobs.id, jobId));

  logger.info({ jobId, title: parsed.data.title }, 'outline done');
}
```

- [ ] **Step 3: Smoke: 起 worker + 提一个 job，看 storage/jobs/{id}/plan.json 落地**

```bash
# terminal 1
npm run worker

# terminal 2
curl -u admin:changeme -X POST http://localhost:3000/api/jobs \
  -H "content-type: application/json" \
  -d '{"inputType":"text","topic":"RAG 工作原理"}'
sleep 15
ls storage/jobs/*/plan.json
```

Expected: 一个 `plan.json` 出现，含 5 个 beats 与 visual_plan。

- [ ] **Step 4: Commit**

```bash
git add worker/phases/outline.ts lib/prompts.ts
git commit -m "feat(outline): Claude-powered outline planner + QG-plan v1 (structural)"
```

---

### Task 15: ScriptWriter（per-beat 口播稿）

**Files:**
- Create: `worker/phases/script.ts`

- [ ] **Step 1: 实现**

```typescript
import fs from 'fs/promises';
import path from 'path';
import { eq } from 'drizzle-orm';
import { getDb } from '@/lib/db';
import { jobs } from '@/lib/schema';
import { callClaude } from '@/lib/llm';
import { SYSTEM_PROMPT_HEADER } from '@/lib/prompts';
import { logger } from '@/lib/logger';

const SCRIPT_PROMPT = (planTitle: string, beatName: string, beatPurpose: string) => `
为视频「${planTitle}」的第 ${beatName} 段写一段 6 秒口播稿（约 30 字）。

beat 目的：${beatPurpose}

约束：
- 中文口语，6 秒念完（30 字以内，按 250 字/分钟 算）
- 不要用"大家好"开篇
- 钩子段第 1 句必须有反常识冲击力
- 收尾段必须有一句"关键词 + 一句话本质"

只输出口播稿正文（不要 JSON，不要标签），直接念稿：
`.trim();

export async function phaseScript(jobId: string) {
  const db = getDb();
  const [job] = await db.select().from(jobs).where(eq(jobs.id, jobId)).limit(1);
  if (!job) throw new Error('job not found');

  const planPath = path.join(process.cwd(), 'storage', 'jobs', jobId, 'plan.json');
  const plan = JSON.parse(await fs.readFile(planPath, 'utf8'));

  const jobDir = path.dirname(planPath);
  const scriptLines: { beat_id: string; name: string; text: string }[] = [];

  for (const beat of plan.beats) {
    const text = (await callClaude({
      system: SYSTEM_PROMPT_HEADER,
      messages: [{
        role: 'user',
        content: SCRIPT_PROMPT(plan.title, beat.name, beat.purpose),
      }],
      maxTokens: 256,
    })).trim();

    if (text.length > 60) {
      logger.warn({ jobId, beat: beat.id, len: text.length }, 'script too long, truncated');
      scriptLines.push({ beat_id: beat.id, name: beat.name, text: text.slice(0, 60) });
    } else {
      scriptLines.push({ beat_id: beat.id, name: beat.name, text });
    }
  }

  const scriptPath = path.join(jobDir, 'script.md');
  const md = scriptLines
    .map((l) => `## ${l.beat_id} · ${l.name}\n\n${l.text}\n`)
    .join('\n');
  await fs.writeFile(scriptPath, md, 'utf8');

  logger.info({ jobId, beats: scriptLines.length }, 'script done');
}
```

- [ ] **Step 2: Smoke**

```bash
# 起 worker + 提交 job，等 30s
ls storage/jobs/*/script.md
cat storage/jobs/*/script.md
```

Expected: 5 个 beat 的口播稿段落，每段 ≤ 60 字。

- [ ] **Step 3: Commit**

```bash
git add worker/phases/script.ts
git commit -m "feat(script): per-beat voiceover writer using Claude"
```

---

## Phase 5 — HTML Render + Probe (Tasks 16-18)

### Task 16: 把 30s beat5 模板搬进仓库

**Files:**
- Create: `lib/templates/beat5-30s.html`

- [ ] **Step 1: 从你 `D:\项目\数据库\` 里挑一个 30s 抖音模板，复制并简化**

模板格式契约（必须遵守，HtmlRenderer 会按 placeholder 替换）：

```html
<!DOCTYPE html>
<html lang="zh">
<head>
<meta charset="utf-8">
<title>{{TITLE}}</title>
<style>
  /* 暗底 + 黑描边 Fraunces em 的视觉签名（你 §41 设计签名元素） */
  :root {
    --bg: #0d1117; --fg: #c9d1d9; --accent: #58a6ff; --muted: #8b949e;
    --bignum: #b026ff;
  }
  * { box-sizing: border-box; margin: 0; padding: 0; }
  html, body { width: 1920px; height: 1080px; overflow: hidden; background: var(--bg); }
  body { font-family: ui-sans-serif, "Microsoft YaHei"; color: var(--fg); }
  .beat {
    position: absolute; inset: 0;
    display: flex; flex-direction: column; justify-content: center; align-items: center;
    opacity: 0; visibility: hidden;
    transition: opacity .4s, visibility 0s linear .4s;
    padding: 80px 60px 110px;
    text-align: center;
  }
  .beat.in { opacity: 1; visibility: visible; transition: opacity .4s, visibility 0s linear 0s; }
  .bignum {
    position: absolute; top: 60px; left: 60px;
    font-size: 280px; font-weight: 900; line-height: .8;
    color: var(--bignum); opacity: .022;
    font-family: "Inter", system-ui;
  }
  h1 {
    font-size: clamp(64px, 8vw, 120px);
    color: var(--accent); font-weight: 900;
    text-shadow: -3px -3px 0 #000, 3px -3px 0 #000, -3px 3px 0 #000, 3px 3px 0 #000, 0 0 24px rgba(0,0,0,.5);
    margin-bottom: 32px; letter-spacing: -2px;
  }
  h1 em {
    font-family: "Fraunces", "Source Serif Pro", serif;
    font-style: italic; font-weight: 700;
  }
  p { font-size: clamp(28px, 3vw, 44px); color: var(--fg); line-height: 1.5; max-width: 1500px; }
  .anim { opacity: 0; transform: translateY(40px); transition: opacity .7s, transform .7s; }
  .beat.in .anim.in { opacity: 1; transform: none; }
  .beat.in .anim.d1 { transition-delay: .1s; }
  .beat.in .anim.d2 { transition-delay: .25s; }
  .beat.in .anim.d3 { transition-delay: .4s; }
  .beat.in .anim.d4 { transition-delay: .55s; }
</style>
</head>
<body>
{{BEATS}}

<script>
const BEATS = {{BEATS_JSON}};
let cur = -1;
function show(i) {
  document.querySelectorAll('.beat').forEach((b) => b.classList.remove('in'));
  const el = document.getElementById(BEATS[i].id);
  setTimeout(() => el.classList.add('in'), 50);
  el.querySelectorAll('.anim').forEach((a, idx) => {
    setTimeout(() => a.classList.add('in'), 50 + 250 + idx * 150);
  });
}
function next() {
  cur = (cur + 1) % BEATS.length;
  show(cur);
}
window.__ready = true;

// query ?autoplay=1 → 自动播放时间线
if (location.search.includes('autoplay')) {
  window.__autoplay = true;
  let i = 0;
  async function loop() {
    if (i >= BEATS.length) { window.__done = true; return; }
    show(i);
    await new Promise(r => setTimeout(r, BEATS[i].duration_ms));
    i++;
    loop();
  }
  setTimeout(loop, 500);
}
</script>
</body>
</html>
```

> 这是 `lib/templates/beat5-30s.html` 的文件内容 —— **纯模板，placeholder 是 `{{TITLE}}` / `{{BEATS}}` / `{{BEATS_JSON}}`**。
> 拷贝你 `D:\项目\数据库\列表讲解视频\` 或 `字符串讲解视频\` 里你最满意的 30s 模板内容作底，把 `{{TITLE}}` / `{{BEATS}}` / `{{BEATS_JSON}}` 留好。

- [ ] **Step 2: Commit**

```bash
git add lib/templates/beat5-30s.html
git commit -m "feat(template): 30s beat5 template skeleton with placeholders"
```

---

### Task 17: TemplateSelector (hardcode) + HtmlRenderer

**Files:**
- Create: `worker/phases/html.ts`（覆盖 stub）

- [ ] **Step 1: 实现**

```typescript
import fs from 'fs/promises';
import path from 'path';
import { eq } from 'drizzle-orm';
import { getDb } from '@/lib/db';
import { jobs } from '@/lib/schema';
import { logger } from '@/lib/logger';

export async function phaseHtml(jobId: string) {
  const db = getDb();
  const [job] = await db.select().from(jobs).where(eq(jobs.id, jobId)).limit(1);
  if (!job) throw new Error('job not found');

  // v0.0.1：硬编码一个模板
  const templateId = job.templateId ?? 'beat5-30s';
  if (templateId !== 'beat5-30s') {
    throw new Error(`unknown template ${templateId} (v0.0.1 only beat5-30s)`);
  }

  const jobDir = path.join(process.cwd(), 'storage', 'jobs', jobId);
  const plan = JSON.parse(await fs.readFile(path.join(jobDir, 'plan.json'), 'utf8'));

  // 读模板
  const tplPath = path.join(process.cwd(), 'lib', 'templates', 'beat5-30s.html');
  let tpl = await fs.readFile(tplPath, 'utf8');

  // 替换 BEATS（生成 5 个 .beat 块）
  const beatsHtml = plan.beats
    .map((b: any, i: number) => `
<div class="beat" id="${b.id}">
  <div class="bignum">0${i + 1}</div>
  <h1 class="anim d2">{{BEAT_${i}_TITLE}}</h1>
  <p class="anim d3">{{BEAT_${i}_TEXT}}</p>
</div>`).join('\n');

  // 替换 BEAT 标题 + 文本（来自 plan.visual_plan + script.md）
  const scriptPath = path.join(jobDir, 'script.md');
  const scriptRaw = await fs.readFile(scriptPath, 'utf8');
  const scriptBeats = parseScriptMd(scriptRaw);

  for (let i = 0; i < plan.beats.length; i++) {
    const beat = plan.beats[i];
    const visual = plan.visual_plan.find((v: any) => v.beat_id === beat.id);
    const text = scriptBeats.get(beat.id) ?? '';
    const tplPlaceholder = `{{BEAT_${i}_TITLE}}`;
    // v0.0.1：title 用 beat.name，text 用 script
    tpl = tpl.replace(tplPlaceholder, beat.name);
    tpl = tpl.replace(`{{BEAT_${i}_TEXT}}`, text);
    logger.debug({ beat: beat.id, visual: visual?.visual }, 'beat visual cue');
  }

  tpl = tpl.replace('{{TITLE}}', plan.title);
  tpl = tpl.replace('{{BEATS}}', beatsHtml);
  tpl = tpl.replace('{{BEATS_JSON}}', JSON.stringify(plan.beats.map((b: any) => ({
    id: b.id, name: b.name, duration_ms: b.duration_ms,
  }))));

  // 写产物
  const outPath = path.join(jobDir, 'video.html');
  await fs.writeFile(outPath, tpl, 'utf8');
  await db.insert((await import('@/lib/schema')).jobArtifacts).values({
    jobId, kind: 'html', storagePath: outPath,
  });

  logger.info({ jobId, htmlPath: outPath }, 'html rendered');
}

function parseScriptMd(md: string): Map<string, string> {
  const map = new Map<string, string>();
  const blocks = md.split(/^## /m).slice(1);
  for (const block of blocks) {
    const [headLine, ...rest] = block.split('\n');
    const beatId = headLine.trim().split(' ')[0];
    const text = rest.join('\n').trim();
    map.set(beatId, text);
  }
  return map;
}
```

- [ ] **Step 2: 单元测试 `tests/unit/script-parser.test.ts`**

```typescript
import { describe, it, expect } from 'vitest';
import path from 'path';
import fs from 'fs/promises';

// 简单复制核心解析逻辑到测试（避免动态导入）
function parseScriptMd(md: string): Map<string, string> {
  const map = new Map<string, string>();
  const blocks = md.split(/^## /m).slice(1);
  for (const block of blocks) {
    const [headLine, ...rest] = block.split('\n');
    const beatId = headLine.trim().split(' ')[0];
    const text = rest.join('\n').trim();
    map.set(beatId, text);
  }
  return map;
}

describe('parseScriptMd', () => {
  it('parses 5-beat script', () => {
    const md = `
## b1 · 钩子

你以为 RAG 是魔法？错。

## b2 · 定义

RAG = 检索增强生成。`.trim();
    const map = parseScriptMd(md);
    expect(map.get('b1')).toBe('你以为 RAG 是魔法？错。');
    expect(map.get('b2')).toBe('RAG = 检索增强生成。');
  });
});
```

- [ ] **Step 3: Smoke**

```bash
# 起 worker + 提交 job
ls storage/jobs/*/video.html
```

Expected: `video.html` 落地，文件中无 `{{...}}` placeholder。

- [ ] **Step 4: Commit**

```bash
git add worker/phases/html.ts tests/unit/script-parser.test.ts
git commit -m "feat(html): template renderer + plan-driven beats injection"
```

---

### Task 18: ProbeChecker (Playwright + console)

**Files:**
- Create: `worker/phases/probe.ts`（覆盖 stub）, `tests/unit/probe-ids-check.test.ts`

- [ ] **Step 1: 写失败测试**

```typescript
import { describe, it, expect } from 'vitest';

describe('placeholder id check', () => {
  it('returns missing ids for {{{...}}} unresolved placeholders', () => {
    const html = `<html><body>{{BEAT_0_TITLE}}<div id="b1">{{BEAT_0_TEXT}}</div></body></html>`;
    const re = /\{\{([A-Z_0-9]+)\}\}/g;
    const missing: string[] = [];
    let m;
    while ((m = re.exec(html)) !== null) missing.push(m[1]);
    expect(missing).toEqual(['BEAT_0_TITLE', 'BEAT_0_TEXT']);
  });
});
```

- [ ] **Step 2: 实现 `worker/phases/probe.ts`**

```typescript
import { chromium } from 'playwright';
import fs from 'fs/promises';
import path from 'path';
import { eq } from 'drizzle-orm';
import { getDb } from '@/lib/db';
import { jobs } from '@/lib/schema';
import { logger } from '@/lib/logger';

export async function phaseProbe(jobId: string) {
  const db = getDb();
  const [job] = await db.select().from(jobs).where(eq(jobs.id, jobId)).limit(1);
  const htmlPath = path.join(process.cwd(), 'storage', 'jobs', jobId, 'video.html');
  const html = await fs.readFile(htmlPath, 'utf8');

  // 1. ID 一致性 + 占位符残留检测（你 §20.2 checkIds 原则）
  const placeholderRe = /\{\{([A-Z_0-9]+)\}\}/g;
  const placeholders: string[] = [];
  let m;
  while ((m = placeholderRe.exec(html)) !== null) placeholders.push(m[1]);
  if (placeholders.length > 0) {
    throw new Error(`unresolved placeholders: ${placeholders.join(', ')}`);
  }

  const requiredIds = ['b1', 'b2', 'b3', 'b4', 'b5'];
  for (const id of requiredIds) {
    if (!new RegExp(`id="${id}"`).test(html)) {
      throw new Error(`missing beat id #${id}`);
    }
  }

  // 2. 真实浏览器探针（headless）
  logger.info({ jobId }, 'probe starting');
  const browser = await chromium.launch({
    headless: true,
    executablePath: process.env.CHROME_PATH ?? 'C:/Program Files/Google/Chrome/Application/chrome.exe',
    args: ['--no-sandbox', '--disable-dev-shm-usage'],
  });
  const ctx = await browser.newContext({ viewport: { width: 1920, height: 1080 } });
  const page = await ctx.newPage();

  const consoleErrors: string[] = [];
  const pageErrors: string[] = [];
  page.on('console', (msg) => { if (msg.type() === 'error') consoleErrors.push(msg.text()); });
  page.on('pageerror', (err) => pageErrors.push(err.message));

  await page.goto('file:///' + htmlPath.replace(/\\/g, '/'), { waitUntil: 'load' });
  await page.waitForFunction(() => (window as any).__ready === true, { timeout: 5000 });

  // 强制翻到第 1 屏 + 检查 class
  await page.evaluate(() => {
    const el = document.getElementById('b1');
    el?.classList.add('in');
  });
  await page.waitForTimeout(500);
  const b1Visible = await page.evaluate(() => document.getElementById('b1')?.classList.contains('in'));
  const b2Missing = await page.evaluate(() => !document.getElementById('b2'));

  await browser.close();

  if (consoleErrors.length > 0) throw new Error(`console errors: ${consoleErrors.join('; ')}`);
  if (pageErrors.length > 0) throw new Error(`page errors: ${pageErrors.join('; ')}`);
  if (!b1Visible) throw new Error('b1 did not enter visible state');
  if (b2Missing) throw new Error('b2 element missing at runtime');

  logger.info({ jobId }, 'probe passed');
}
```

- [ ] **Step 3: 跑测试 pass**

```bash
npm run test
```

Expected: 全部 PASS（含新增 placeholder id check）。

- [ ] **Step 4: Smoke**

```bash
# 起 worker + 提交 job
# 看 log 出现 "probe passed"
```

- [ ] **Step 5: Commit**

```bash
git add worker/phases/probe.ts tests/unit/probe-ids-check.test.ts
git commit -m "feat(probe): playwright console check + ID consistency gate"
```

---

## Phase 6 — Recording + Encode (Tasks 19-22)

### Task 19: Recorder (puppeteer-core + CDP screencast)

**Files:**
- Create: `worker/phases/record.ts`（覆盖 stub）

- [ ] **Step 1: 实现**

```typescript
import puppeteer from 'puppeteer-core';
import fs from 'fs/promises';
import path from 'path';
import { logger } from '@/lib/logger';

const CHROME_PATH = process.env.CHROME_PATH ?? 'C:/Program Files/Google/Chrome/Application/chrome.exe';

export async function phaseRecord(jobId: string) {
  const jobDir = path.join(process.cwd(), 'storage', 'jobs', jobId);
  const htmlPath = path.join(jobDir, 'video.html');
  const framesDir = path.join(jobDir, 'frames');
  await fs.rm(framesDir, { recursive: true, force: true });
  await fs.mkdir(framesDir, { recursive: true });

  const htmlUrl = 'file:///' + htmlPath.replace(/\\/g, '/');

  const browser = await puppeteer.launch({
    executablePath: CHROME_PATH,
    headless: 'new',
    args: ['--no-sandbox', '--disable-setuid-sandbox', '--disable-gpu', '--hide-scrollbars'],
  });

  const page = await browser.newPage();
  await page.setViewport({ width: 1920, height: 1080, deviceScaleFactor: 1 });
  await page.goto(htmlUrl, { waitUntil: 'networkidle0' });
  await page.evaluate(() => {
    history.replaceState({}, '', location.pathname + '?autoplay=1');
    location.reload();
  });
  // 等 ?autoplay=1 触发
  await new Promise((r) => setTimeout(r, 800));
  await page.waitForFunction(() => (window as any).__ready === true, { timeout: 5000 });

  // CDP screencast (60fps target)
  const client = await page.target().createCDPSession();
  await client.send('Page.startScreencast', {
    format: 'png', quality: 85,
    maxWidth: 1920, maxHeight: 1080, everyNthFrame: 1,
  });

  let frameIdx = 0;
  await new Promise<void>((resolve) => {
    client.on('Page.screencastFrame', async ({ data, sessionId }) => {
      const filename = path.join(framesDir, `f_${String(frameIdx).padStart(5, '0')}.png`);
      await fs.writeFile(filename, Buffer.from(data, 'base64'));
      frameIdx++;
      await client.send('Page.screencastFrameAck', { sessionId });
    });
    // 总时长 32s（30s + 2s buffer）
    setTimeout(resolve, 32000);
  });

  await client.send('Page.stopScreencast');
  await browser.close();

  logger.info({ jobId, frames: frameIdx }, 'recording done');
}
```

- [ ] **Step 2: Commit**

```bash
git add worker/phases/record.ts
git commit -m "feat(record): puppeteer-core + CDP screencast at 60fps target"
```

---

### Task 20: FpsCalculator (mtime 算法 — 你 §54)

**Files:**
- Create: `worker/phases/fps.ts`, `tests/unit/fps.test.ts`

- [ ] **Step 1: 写失败测试**

```typescript
import { describe, it, expect } from 'vitest';
import { computeRealFps } from '@/worker/phases/fps';

describe('computeRealFps', () => {
  it('computes fps from frames with 1ms apart, 30 frames', () => {
    const now = Date.now();
    const frames = Array.from({ length: 30 }, (_, i) => ({
      name: `f_${String(i).padStart(5, '0')}.png`,
      mtimeMs: now + i,
    }));
    expect(computeRealFps(frames)).toBeCloseTo(30, 0);
  });

  it('returns 0 for empty', () => {
    expect(computeRealFps([])).toBe(0);
  });

  it('returns 0 for identical mtimes', () => {
    expect(computeRealFps([{ name: 'a', mtimeMs: 100 }, { name: 'b', mtimeMs: 100 }])).toBe(0);
  });
});
```

- [ ] **Step 2: 跑测试 fail**

```bash
npm run test -- tests/unit/fps.test.ts
```

Expected: FAIL。

- [ ] **Step 3: 实现 `worker/phases/fps.ts`**

```typescript
import fs from 'fs/promises';
import path from 'path';

export interface FrameMeta {
  name: string;
  mtimeMs: number;
}

export async function loadFramesMeta(framesDir: string): Promise<FrameMeta[]> {
  const files = (await fs.readdir(framesDir))
    .filter((f) => f.startsWith('f_') && f.endsWith('.png'))
    .sort();
  const stats = await Promise.all(
    files.map(async (name) => {
      const s = await fs.stat(path.join(framesDir, name));
      return { name, mtimeMs: s.mtimeMs };
    }),
  );
  return stats;
}

export function computeRealFps(frames: FrameMeta[]): number {
  if (frames.length < 2) return 0;
  const first = frames[0].mtimeMs;
  const last = frames[frames.length - 1].mtimeMs;
  const wallSec = (last - first) / 1000;
  if (wallSec <= 0) return 0;
  return Math.round((frames.length / wallSec) * 10) / 10;
}
```

- [ ] **Step 4: 跑测试 pass**

```bash
npm run test
```

Expected: 全部 PASS。

- [ ] **Step 5: Commit**

```bash
git add worker/phases/fps.ts tests/unit/fps.test.ts
git commit -m "feat(fps): mtime-based real fps calculator (per 通用模式 §54)"
```

---

### Task 21: Encoder (ffmpeg-static + H.264)

**Files:**
- Create: `worker/phases/encode.ts`（覆盖 stub）, `tests/unit/encode-cmd.test.ts`

- [ ] **Step 1: 写失败测试**

```typescript
import { describe, it, expect } from 'vitest';
import { buildEncodeArgs } from '@/worker/phases/encode';

describe('buildEncodeArgs', () => {
  it('produces args for input fps + output 30fps', () => {
    const args = buildEncodeArgs({
      inputFps: 18.3,
      framesPattern: '/tmp/job-x/frames/f_%05d.png',
      outputPath: '/tmp/job-x/video.mp4',
    });
    expect(args).toContain('-framerate');
    expect(args).toContain('18.3');
    expect(args).toContain('-vf');
    expect(args).toContain('fps=30');
    expect(args).toContain('/tmp/job-x/video.mp4');
  });
});
```

- [ ] **Step 2: 实现 `worker/phases/encode.ts`**

```typescript
import { spawn } from 'child_process';
import ffmpegPath from 'ffmpeg-static';
import path from 'path';
import fs from 'fs/promises';
import { loadFramesMeta, computeRealFps } from './fps';
import { logger } from '@/lib/logger';

export function buildEncodeArgs(opts: {
  inputFps: number;
  framesPattern: string;
  outputPath: string;
}): string[] {
  return [
    '-y',
    '-framerate', String(opts.inputFps),
    '-i', opts.framesPattern,
    '-vf', 'fps=30',
    '-c:v', 'libx264',
    '-pix_fmt', 'yuv420p',
    '-preset', 'slow',
    '-crf', '20',
    '-movflags', '+faststart',
    opts.outputPath,
  ];
}

export async function phaseEncode(jobId: string) {
  const jobDir = path.join(process.cwd(), 'storage', 'jobs', jobId);
  const framesDir = path.join(jobDir, 'frames');
  const frames = await loadFramesMeta(framesDir);
  const realFps = computeRealFps(frames);
  logger.info({ jobId, realFps, count: frames.length }, 'encode starting');

  const outPath = path.join(jobDir, 'video.mp4');
  const args = buildEncodeArgs({
    inputFps: realFps,
    framesPattern: path.join(framesDir, 'f_%05d.png'),
    outputPath: outPath,
  });

  await new Promise<void>((resolve, reject) => {
    const proc = spawn(ffmpegPath as unknown as string, args, { stdio: 'inherit' });
    proc.on('exit', (code) => (code === 0 ? resolve() : reject(new Error(`ffmpeg exit ${code}`))));
    proc.on('error', reject);
  });

  // 写 artifact 记录
  const sizeBytes = (await fs.stat(outPath)).size;
  const { getDb } = await import('@/lib/db');
  const { jobArtifacts, jobs } = await import('@/lib/schema');
  const { eq } = await import('drizzle-orm');
  const db = getDb();
  await db.insert(jobArtifacts).values({
    jobId, kind: 'mp4', storagePath: outPath, sizeBytes,
  });
  await db.update(jobs).set({ templateId: 'beat5-30s' }).where(eq(jobs.id, jobId));

  // 清理 frames（节省磁盘）
  await fs.rm(framesDir, { recursive: true, force: true });

  logger.info({ jobId, outPath, sizeBytes }, 'encode done');
}
```

- [ ] **Step 3: 跑测试 pass**

```bash
npm run test
```

Expected: 全部 PASS。

- [ ] **Step 4: Smoke**

```bash
ffmpeg -version
# 应该能跑 ffmpeg-static
```

- [ ] **Step 5: Commit**

```bash
git add worker/phases/encode.ts tests/unit/encode-cmd.test.ts
git commit -m "feat(encode): ffmpeg-static wrapper + dynamic fps from mtime"
```

---

### Task 22: Pipeline smoke 端到端

**Files:**
- Create: `tests/integration/pipeline-smoke.test.ts`

- [ ] **Step 1: 写完整 smoke test**

```typescript
import { describe, it, expect, beforeAll } from 'vitest';
import fs from 'fs/promises';
import path from 'path';

beforeAll(() => {
  if (!process.env.ANTHROPIC_API_KEY) {
    throw new Error('ANTHROPIC_API_KEY required for pipeline smoke test');
  }
});

describe('pipeline smoke', () => {
  it('runs a job end-to-end producing mp4', async () => {
    // 这个测试作为 optional slow test，默认 skip
    // 启用：ANTHROPIC_API_KEY=... RUN_SLOW_TESTS=1 npm run test -- pipeline-smoke
    if (!process.env.RUN_SLOW_TESTS) {
      console.log('skip: set RUN_SLOW_TESTS=1 to enable');
      return;
    }

    // 直接调 runPipeline + 检查 mp4 落地
    const { runPipeline } = await import('@/worker/pipeline');
    const { getDb } = await import('@/lib/db');
    const { jobs } = await import('@/lib/schema');
    const db = getDb();
    const [job] = await db.insert(jobs).values({
      userId: 'admin',
      inputType: 'text',
      inputPayload: { topic: '测试 RAG 原理 alpha' },
    }).returning();
    const jobId = job.id;

    await runPipeline(jobId);

    const outPath = path.join(process.cwd(), 'storage', 'jobs', jobId, 'video.mp4');
    const stat = await fs.stat(outPath);
    expect(stat.size).toBeGreaterThan(10_000);

    const [updated] = await db.select().from(jobs).where((j: any) => j.id as any).limit(1);
    expect(updated.status).toBe('done');
  }, { timeout: 600_000 });
});
```

- [ ] **Step 2: 验证基础设施 OK（不在 CI 跑，靠人工）**

```bash
docker compose up -d
npm run dev &
npm run worker &
sleep 5
# 提交
curl -u admin:changeme -X POST http://localhost:3000/api/jobs \
  -H "content-type: application/json" \
  -d '{"inputType":"text","topic":"RAG 工作原理"}'
sleep 90
ls -la storage/jobs/*/video.mp4
```

Expected: `video.mp4` 存在（数 MB），job 状态 `done`。

- [ ] **Step 3: Commit**

```bash
git add tests/integration/pipeline-smoke.test.ts
git commit -m "test: end-to-end pipeline smoke (manual run with RUN_SLOW_TESTS=1)"
```

---

## Phase 7 — Delivery (Tasks 23-24)

### Task 23: Download API

**Files:**
- Create: `app/api/jobs/[id]/download/[kind]/route.ts`

- [ ] **Step 1: 实现**

```typescript
import { NextResponse } from 'next/server';
import { eq } from 'drizzle-orm';
import fs from 'fs/promises';
import path from 'path';
import { getDb } from '@/lib/db';
import { jobs } from '@/lib/schema';
import { createBasicAuthMiddleware, unauthorizedResponse } from '@/lib/auth';
import { getEnv } from '@/lib/env';

const authMw = createBasicAuthMiddleware({
  user: getEnv().BASIC_AUTH_USER,
  pass: getEnv().BASIC_AUTH_PASS,
});

export async function GET(
  req: Request,
  { params }: { params: { id: string; kind: string } },
) {
  if (!authMw(req)) return unauthorizedResponse();
  if (params.kind !== 'mp4') return new Response('not supported in v0.0.1', { status: 400 });

  const db = getDb();
  const [job] = await db.select().from(jobs).where(eq(jobs.id, params.id)).limit(1);
  if (!job || job.status !== 'done') return new Response('not ready', { status: 404 });

  const filePath = path.join(process.cwd(), 'storage', 'jobs', params.id, 'video.mp4');
  try {
    const data = await fs.readFile(filePath);
    return new Response(data, {
      headers: {
        'content-type': 'video/mp4',
        'content-disposition': `attachment; filename="video-${params.id.slice(0, 8)}.mp4"`,
      },
    });
  } catch {
    return new Response('file missing', { status: 404 });
  }
}
```

- [ ] **Step 2: Commit**

```bash
git add app/api/jobs/[id]/download/[kind]/route.ts
git commit -m "feat(api): GET /api/jobs/[id]/download/mp4 (v0.0.1 only)"
```

---

### Task 24: 验收 — 部署到 VPS + "RAG 原理" golden

**Files:**
- Modify: `package.json` (add `deploy` script)
- Create: `scripts/deploy.sh`

- [ ] **Step 1: 创建 `scripts/deploy.sh`**

```bash
#!/usr/bin/env bash
set -euo pipefail
# 复用你卡通大富翁的 tarball + atomic symlink 模式

VPS="${VPS:-user@vps-host}"
APP_DIR="/srv/auto-explainer"
RELEASE_DIR="$APP_DIR/releases/v$(date +%Y%m%d%H%M%S)"
CURRENT_LINK="$APP_DIR/current"

echo "📦 Building release…"
cd "$(dirname "$0")/.."
npm ci --omit=dev

echo "🗜️  Creating tarball…"
TAR="/tmp/auto-explainer-v$(date +%Y%m%d%H%M%S).tar.gz"
tar czf "$TAR" --exclude=node_modules --exclude=.next --exclude=storage/jobs/* .

echo "🚀 Uploading to VPS…"
ssh "$VPS" "mkdir -p $APP_DIR/releases $APP_DIR/shared"

scp "$TAR" "$VPS:/tmp/"
ssh "$VPS" "mkdir -p $RELEASE_DIR && tar xzf /tmp/$(basename $TAR) -C $RELEASE_DIR"

echo "🔗 Atomic symlink swap…"
ssh "$VPS" "ln -sfn $RELEASE_DIR $APP_DIR/current.new && mv -Tf $APP_DIR/current.new $APP_DIR/current"

echo "🔄 Restarting services…"
ssh "$VPS" "sudo systemctl restart auto-explainer-web auto-explainer-worker"

echo "✅ Health check…"
sleep 3
curl -sf http://127.0.0.1:3000/api/health || (echo "❌ health check failed" && exit 1)

echo "🧹 Cleanup old releases (keep 3)…"
ssh "$VPS" "cd $APP_DIR/releases && ls -t | tail -n +4 | xargs -r rm -rf"

echo "✅ Done."
```

- [ ] **Step 2: 加 `package.json` script**

```json
{
  "scripts": {
    "deploy": "bash scripts/deploy.sh"
  }
}
```

- [ ] **Step 3: 验证本地完整流程**

```bash
# 三终端：
# t1: docker compose up -d
# t2: npm run dev
# t3: npm run worker
# t4: 提交
curl -u admin:changeme -X POST http://localhost:3000/api/jobs \
  -H "content-type: application/json" \
  -d '{"inputType":"text","topic":"RAG 工作原理"}'

# 记 jobId, 每 30s 看 status
curl -u admin:changeme http://localhost:3000/api/jobs/{jobId}

# 等到 status=done 后下载
curl -u admin:changeme -o video.mp4 http://localhost:3000/api/jobs/{jobId}/download/mp4

# 用 ffprobe 验证
ffprobe video.mp4 2>&1 | grep -E "Duration|Stream"
# Duration 应该 ~30s
# Stream 应该是 H.264
```

Expected:
- 视频时长 ≈ 30 ± 3s
- 编码 H.264 / yuv420p / 30fps
- 文件大小 1-5MB
- 5 个 beats 都呈现（手动看）

- [ ] **Step 4: 推 VPS（按需，不在本 plan 必做）**

> v0.0.1 不强制 VPS 部署。如果 staging 也只是本地跑就够。spec §7.5 v0.0.1 验收是"你能从浏览器提交 + 拿到 mp4"，本地 OK。

- [ ] **Step 5: Commit**

```bash
git add scripts/deploy.sh package.json
git commit -m "chore: deploy script (tarball + atomic symlink, mirrors life-sim pattern)"
```

- [ ] **Step 6: 提 tag + 完成 v0.0.1**

```bash
git tag v0.0.1
git log --oneline | head -30
```

---

## Self-Review

跑了一遍 spec vs plan 一致性检查：

**1. Spec coverage**:
| Spec 节 | 任务覆盖 |
|---|---|
| §0 Executive Summary | Task 24 acceptance |
| §1.1 三层拓扑（控制面 / worker / 外部） | Task 1 / 10 |
| §1.2 数据流（topic → mp4） | Task 6 → 22 |
| §1.3 关键抉择（puppeteer-core / ffmpeg / Claude / Next.js / Postgres / Redis） | 全部命中 |
| §1.4 非目标（i18n / 协作 / avatar） | 未触碰，符合 v0.0.1 范围 |
| §2 组件 (AuthService / JobIntake / JobStatus / LLM / Recorder / Encoder / Storage) | Task 5 / 6 / 7 / 13 / 19 / 21 / 23 |
| §3.1 状态机 | Task 11（v0.0.1 简化版） |
| §3.2 schema | Task 3 |
| §3.4 幂等 | Task 6（client-side button disable） + Redis dedup 留 v0.5 |
| §3.5 时长预算 8-20min | Task 24 验收对齐 |
| §4.2 QG-plan | Task 14（基础结构检查） |
| §4.3 自动降级链 | v0.0.1 不全做（GPT-4o fallback 留 v0.5） |
| §4.4 Human-in-loop | v0.0.1 不做（QG 失败直接 throw） |
| §4.5 Anti-patterns | Task 18（placeholder check）+ probe-console |
| §4.6 LLM prompt 头三条 | Task 14 在 SYSTEM_PROMPT_HEADER 应用 |
| §5 错误处理 | Task 11（catch + status=failed） |
| §6 多租户 / 计费 / 安全 | v0.0.1 仅 basic auth（Task 5），其余 v0.5+ |
| §7.5 v0.0.1 验收 | Task 24 完整覆盖 |
| 附录 B 通用模式引用 (§23.2 / §54 / §39 / §4 / §27) | Task 19 / 20 / 21 / 17 |

**Gap analysis**:
- ❌ §3.2 schema 中 `plans` 表和 `quota_periods` 表 — v0.0.1 不需要（无 billing），保留 v0.5 建
- ❌ §4.6 system prompt 三条全部应用 — Task 14/15 已用，Task 17/18 复用同一 header
- ❌ §5.4 不删无同意（资产 TTL） — v0.0.1 不引入（资产在本地 fs，未涉及云端）
- ❌ §6.4 RLS — v0.0.1 单 admin user，不引入

所有 spec 中的 v0.0.1 范围项都已映射到任务。

**2. Placeholder scan**: 没有 `TBD` / `TODO` / "implement later"。所有 step 都含完整代码或具体命令。

**3. Type consistency**:
- `jobs.phase` enum ↔ Task 11 phase 字符串：一致（'planning_done' / 'html_ready' / 'probing' / 'recording_done' / 'done'）
- `PlanSchema` 在 Task 14 定义；Task 17 消费 `plan.beats` 和 `plan.visual_plan` — 一致
- `frameIdx` 格式 `f_%05d.png` 在 Task 19 写出，Task 21 `framesPattern: path.join(framesDir, 'f_%05d.png')` 消费 — 一致
- `realFps` 在 Task 20 计算，Task 21 传 `inputFps: realFps` — 一致
- `jobArtifacts` 表在 Task 3 / 17 / 21 三处写入 — 一致

**4. No "Similar to Task N"**: 每个 task 独立完整，无 cross-reference 偷懒。

---

## Open Items / Future Plans

| Plan | 范围 | 何时写 |
|---|---|---|
| v0.5 (closed beta) | 3 模板 + URL/Doc 输入 + 多阶段 retry + 真正 auth + 灾备演练 | v0.0.1 验收后 |
| v1.0 (public launch) | Stripe 计费 + landing page + 完整 anti-pattern 守卫 + 5 golden topic | v0.5 验收后 |
| v1.5 (国内化) | 微信/支付宝 + i18n + 多人 review | v1.0 上线 4 周内 |
| v2 | 自定义模板 + 团队 + 企业 API | v1.5 后评估 |

---

**End of plan.**

Plan saved to: `D:\项目\系统\docs\superpowers\plans\2026-07-25-auto-explainer-saas-v0_0_1.md`

24 tasks · 5 phases · 完整 TDD 路径 · 零 placeholder。
